<p id="notice"><%= notice %></p>

<div class="container" id="upcomingonevent_div">
    <h1 id="Upcomingevent_heading">Upcoming Events</h1>
</div>
<!--  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>City</th>
      <th>Province</th>
      <th>Postal code</th>
      <th>Country</th>
      <th>Event date</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>-->
<!-- Events showing for Admin -->
<div class="container">
  <div class ="row">
    <% if (user_signed_in? && current_user.admin?) %>
  
    <% @events.each do |event| %>
     
        
        <!--<td><%= event.address %></td>
        
        <td><%= event.province %></td>
        <td><%= event.postal_code %></td>
        <td><%= event.country %></td>
        <td><%= event.event_date %></td>
        <td></td>-->
     
      <% if(!event.status) %>
      <table>
    <div class="col-md-4" id="event_panel">
      <div class="panel panel-success">
        <div class="panel-heading">
          <h4><i class="fa fa-fw fa-check"></i><%= event.name %></h4>
        </div>
        <div class="panel-body">
          <p><b><%= event.city %></b></p>
          <p><%=  truncate(event.description, :length => 50, :separator => '') %></p>
          <a id="more_about_event_button" <%= link_to 'More About Event', event %> </a>
          <a class="btn btn-success"  <%= link_to 'Edit', edit_event_path(event) %> </a>
        </div>
      </div>
    </div>
    </table>
    <% end %>
    <% end %>        
  </div>
  <% end %>
</div>

<!-- Events showing for Users -->
 <div class="container">
  <div class ="row">
   <% if (!user_signed_in? || (user_signed_in? && !current_user.admin?)) %>
  
    <% @events.each do |event| %>
      
        
        <!--<td><%= event.address %></td>
        
        <td><%= event.province %></td>
        <td><%= event.postal_code %></td>
        <td><%= event.country %></td>
        <td><%= event.event_date %></td>
        <td></td>-->
     
      <% if(event.status) %>
      <table>
    <div class="col-md-4" id="event_panel">
      <div class="panel panel-info">
        <div class="panel-heading">
          <h4><i class="fa fa-fw fa-check"></i><%= event.name %></h4>
        </div>
        <div class="panel-body">
          <p><b><%= event.city %></b></p>
          <p><%=  truncate(event.description, :length => 50, :separator => '') %></p>
          <a id="more_about_event_button" <%= link_to 'More About Event', event %> </a>
        </div>
      </div>
    </div>
    </table>
    <% end %>
    <% end %>        
  </div>
  <% end %>
</div>
 







