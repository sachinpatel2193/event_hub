<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name, :id => "edit_user_name" %><br>
    <%= f.text_field :name , :class => "form-control" %>
  </div>
  
  <div class="actions" >
        <%= f.submit :class =>"btn btn-danger", :id => "user_button" %>
        <%= link_to 'Show', @user , :class => "btn btn-primary" , :id => "edit_showbutton"%> 
        <%= link_to 'Back', users_path , :class => "btn btn-success" , :id => "edit_showbutton" %>
   
  </div>
<% end %>
